% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting_helpers.R
\name{get_df_breakdown_compare}
\alias{get_df_breakdown_compare}
\title{Compare Unique-People and Per-Record Breakdowns Side by Side}
\usage{
get_df_breakdown_compare(input_df, year, col)
}
\arguments{
\item{input_df}{a data frame containing at least the columns `fis_year_`,
`person_id`, and `col`}

\item{year}{the fiscal year to filter to}

\item{col}{the name (as a string) of the categorical column to break down}
}
\value{
a data frame with columns `col`, `n_people`, `pct_people`,
  `n_records`, `pct_records`, and `records_per_person`, sorted by
  `n_people` (descending)
}
\description{
Runs [get_df_breakdown()] both ways for the same column and joins the
results, so you can see how many distinct people fall into each level, how
many records they account for, and the ratio between the two. The ratio is
a measure of usage intensity: how many times the average person in that
group used the service that year.
}
