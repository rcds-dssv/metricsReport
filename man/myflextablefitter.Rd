% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting_helpers.R
\name{myflextablefitter}
\alias{myflextablefitter}
\title{Fit a Flextable to a Maximum Width for PDF Output}
\usage{
myflextablefitter(
  ft,
  max_width = 6.2,
  base_font = 8,
  min_font = 6,
  padding_in = 0.06
)
}
\arguments{
\item{ft}{a flextable object}

\item{max_width}{total table width, in inches}

\item{base_font}{starting font size, in points}

\item{min_font}{smallest font size to allow; not enforced by flextable, informational only}

\item{padding_in}{cell padding, in inches}
}
\value{
a flextable object resized to fit within `max_width`
}
\description{
Rescales column widths (proportional to each column's max character count,
considering both header and body text) so the table fits within
`max_width`, then shrinks the font further via [flextable::fit_to_width()]
if still needed. Intended for PDF/LaTeX output, where flextable does not
autofit within the page width the way it does for HTML.
}
